# Proposal: <功能或产品能力名称>

## 1. 元信息

- Status: Draft
- Owner:
- Created:
- Updated:
- Related Product Spec Sections:
- Target Product Surface: Local App / Server Web Admin / Gateway Core / API / MCP / Other

Status 可选值：

- `Draft`：正在讨论，不能作为实现依据。
- `Accepted`：产品方向已确认，可以拆 implementation plan。
- `Implemented`：代码已实现，但尚未合并进主 SPEC 或发布。
- `Merged`：已合并进主产品规格，本文仅作历史记录。
- `Superseded`：已被其他 proposal 或 SPEC 内容替代。
- `Rejected`：明确不做。

## 2. 背景

说明为什么需要这个 proposal。重点写用户问题、业务价值、当前产品规格中的缺口，不要先写技术方案。

## 3. 用户场景

列出这个能力要服务的真实使用场景。

- 作为 `<用户类型>`，我希望 `<能力>`，以便 `<业务结果>`。

## 4. 产品目标

用可验证的方式描述要达成什么。

- 目标 1
- 目标 2
- 目标 3

## 5. 非目标

明确本 proposal 不解决什么，避免实现时膨胀。

- 非目标 1
- 非目标 2

## 6. 产品规格

描述用户可见行为、配置入口、权限规则、错误处理和边界条件。

### 6.1 用户流程

说明用户如何配置、使用、排查和关闭该能力。

### 6.2 配置模型

列出用户需要配置的对象和字段。这里只写产品层字段，不要求等同数据库 schema。

### 6.3 API / 协议行为

说明外部 API、内部协议或兼容性要求。

### 6.4 UI 行为

说明 Local App 和 Server Web Admin 中需要出现的页面、状态和操作。

### 6.5 权限与安全

说明 Admin、Client Key、Credential、日志脱敏、密钥保存、隔离边界等要求。

### 6.6 日志、用量与可观测性

说明需要记录哪些事件、指标和错误，哪些内容必须脱敏。

## 7. 与主产品规格的关系

说明这个 proposal 如何影响主产品规格。

- 新增章节：
- 修改章节：
- 不影响章节：

当 proposal 被接受并稳定后，应把权威内容合并进主产品规格，并将本文状态改为 `Merged`。如果代码已完成但规格尚未合并或尚未发布，可以先标记为 `Implemented`。

## 8. 技术影响

只写足以评估成本和风险的技术影响，不在 proposal 阶段写完整实现计划。

- Core 影响：
- Local App 影响：
- Server 影响：
- 数据存储影响：
- API 影响：
- 测试影响：

## 9. 边界情况

列出容易被忽略的失败场景和产品行为。

- 情况 1：
- 情况 2：

## 10. 待确认问题

只保留真正会影响产品或实现边界的问题。

- 问题 1：
- 问题 2：

## 11. 接受标准

当这些条件满足时，本 proposal 可以进入实现计划或合并进主产品规格。

- 标准 1
- 标准 2
- 标准 3
